{
  "name": "wdio-browserstack-sample",
  "version": "1.0.0",
  "description": "Sample WebdriverIO + BrowserStack project running 10 tests in parallel across 2 devices (5 each, randomized split)",
  "scripts": {
    "test": "wdio run ./wdio.conf.js",
    "test:preferred": "node run-tests.js"
  },
  "devDependencies": {
    "@wdio/browserstack-service": "^9.0.0",
    "@wdio/cli": "^9.0.0",
    "@wdio/local-runner": "^9.0.0",
    "@wdio/mocha-framework": "^9.0.0",
    "@wdio/spec-reporter": "^9.0.0",
    "chai": "^4.4.1"
  },
  "dependencies": {
    "glob": "^13.0.6"
  }
}
